# A股股票及ETF均线监控技术方案

## 1. 技术架构

### 1.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                      Stock Monitor System                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    │
│  │   Config    │    │  Scheduler   │    │ Notification │    │
│  │  (YAML)     │───▶│  (schedule)  │───▶│  (Telegram)  │    │
│  └──────────────┘    └──────────────┘    └──────────────┘    │
│         │                   │                   │              │
│         ▼                   ▼                   ▼              │
│  ┌──────────────┐    ┌──────────────┐                           │
│  │   AkShare   │    │   Strategy   │                           │
│  │  (Data API) │───▶│  (MA Cross)  │                           │
│  └──────────────┘    └──────────────┘                           │
│                           │                                      │
│                           ▼                                      │
│                    ┌──────────────┐                             │
│                    │   Storage    │                             │
│                    │   (JSON)     │                             │
│                    └──────────────┘                             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈选型

| 层级 | 技术方案 | 选型理由 |
|------|----------|----------|
| **编程语言** | Python 3.10+ | 量化交易生态丰富，AkShare/Tushare等成熟库 |
| **数据源** | AkShare | 免费开源，数据源为新浪/东方财富，支持实时+历史 |
| **定时任务** | schedule 库 | 简单可靠，满足10分钟间隔需求 |
| **通知服务** | Telegram Bot | 免费、稳定、即时推送、无需企业认证 |
| **状态存储** | JSON 文件 | 轻量级，无需额外数据库 |
| **配置管理** | PyYAML | 结构化配置，易于维护 |

---

## 2. 核心模块设计

### 2.1 模块结构

```
stock-monitor/
├── config/
│   ├── __init__.py
│   └── loader.py          # 配置文件加载器
├── core/
│   ├── __init__.py
│   ├── fetcher.py         # 行情数据获取
│   ├── indicator.py       # 技术指标计算
│   └── detector.py        # 信号检测逻辑
├── notification/
│   ├── __init__.py
│   └── telegram.py        # Telegram通知
├── storage/
│   ├── __init__.py
│   └── state.py           # 状态持久化
├── scheduler/
│   ├── __init__.py
│   └── runner.py          # 定时任务调度
├── main.py                # 入口文件
├── config.yaml            # 配置文件
├── requirements.txt       # 依赖
└── run.sh                 # 启动脚本
```

### 2.2 核心类设计

#### 2.2.1 数据获取模块 (fetcher.py)

```python
import akshare as ak

class StockFetcher:
    """A股股票数据获取器"""
    
    @staticmethod
    def get_realtime_price(symbol: str) -> float:
        """
        获取实时价格
        
        Args:
            symbol: 股票代码，如 "600519" 或 "sh600519"
        Returns:
            当前价格
        """
        # AkShare 接口
        df = ak.stock_zh_a_spot_em()
        stock = df[df['代码'] == symbol]
        if stock.empty:
            raise ValueError(f"未找到股票: {symbol}")
        return float(stock.iloc[0]['最新价'])
    
    @staticmethod
    def get_historical_prices(symbol: str, period: int = 25) -> pd.DataFrame:
        """
        获取历史K线数据
        
        Args:
            symbol: 股票代码
            period: 获取天数（默认25天，确保有足够数据计算MA20）
        Returns:
            包含日期、收盘价的DataFrame
        """
        # 格式转换：需要添加市场前缀
        if symbol.startswith('6'):
            market_code = f"sh{symbol}"
        else:
            market_code = f"sz{symbol}"
        
        df = ak.stock_zh_a_hist(symbol=market_code, period="daily", 
                                adjust="qfq", start_date="20200101")
        return df.tail(period)
```

#### 2.2.2 技术指标模块 (indicator.py)

```python
import pandas as pd

class TechnicalIndicator:
    """技术指标计算"""
    
    @staticmethod
    def calculate_ma(prices: pd.Series, period: int = 20) -> float:
        """
        计算移动平均线
        
        Args:
            prices: 收盘价序列
            period: 均线周期
        Returns:
            MA值
        """
        return prices.rolling(window=period).mean().iloc[-1]
    
    @staticmethod
    def calculate_ma_series(prices: pd.Series, period: int = 20) -> pd.Series:
        """计算MA序列（用于画图）"""
        return prices.rolling(window=period).mean()
```

#### 2.2.3 信号检测模块 (detector.py)

```python
from typing import Literal
from dataclasses import dataclass

@dataclass
class Signal:
    """交易信号"""
    symbol: str
    direction: Literal["breakout", "breakdown"]  # 上穿 / 下穿
    price: float
    ma20: float
    timestamp: str

class SignalDetector:
    """均线穿越信号检测器"""
    
    def detect(self, symbol: str, current_price: float, 
               ma20: float, previous_position: str = None) -> Signal | None:
        """
        检测是否有新信号
        
        Returns:
            Signal对象（如果有新信号），否则返回None
        """
        # 判断当前位置
        current_position = "above" if current_price > ma20 else "below"
        
        # 无历史状态，跳过（首次运行）
        if previous_position is None:
            return None
        
        # 状态未变化，无信号
        if current_position == previous_position:
            return None
        
        # 检测到穿越
        direction = "breakout" if current_position == "above" else "breakdown"
        
        return Signal(
            symbol=symbol,
            direction=direction,
            price=current_price,
            ma20=ma20,
            timestamp=datetime.now().strftime("%Y-%m-%d %H:%M:%S")
        )
```

#### 2.2.4 通知模块 (telegram.py)

```python
import os
import requests
from typing import Literal

class TelegramNotifier:
    """Telegram通知器"""
    
    def __init__(self, token: str, chat_id: str):
        self.token = token
        self.chat_id = chat_id
        self.api_url = f"https://api.telegram.org/bot{token}"
    
    def send(self, message: str, parse_mode: str = "Markdown") -> bool:
        """发送消息"""
        url = f"{self.api_url}/sendMessage"
        payload = {
            "chat_id": self.chat_id,
            "text": message,
            "parse_mode": parse_mode
        }
        response = requests.post(url, json=payload)
        return response.status_code == 200
    
    def send_signal(self, signal: Signal):
        """发送交易信号"""
        emoji = "🚀" if signal.direction == "breakout" else "🔻"
        direction_text = "突破" if signal.direction == "breakout" else "跌破"
        
        message = f"""
{emoji} *{signal.symbol}* {direction_text} 20日均线

• 现价: `{signal.price:.2f}`
• MA20: `{signal.ma20:.2f}`
• 时间: {signal.timestamp}
"""
        self.send(message)
```

#### 2.2.5 状态存储模块 (state.py)

```python
import json
from pathlib import Path
from typing import Dict, Any

class StateStorage:
    """状态持久化存储"""
    
    def __init__(self, file_path: str = "data/state.json"):
        self.file_path = Path(file_path)
        self.file_path.parent.mkdir(parents=True, exist_ok=True)
    
    def load(self) -> Dict[str, Any]:
        """加载状态"""
        if not self.file_path.exists():
            return {}
        
        try:
            with open(self.file_path, 'r', encoding='utf-8') as f:
                return json.load(f)
        except (json.JSONDecodeError, IOError):
            # 文件损坏，返回空状态
            return {}
    
    def save(self, states: Dict[str, Any]):
        """保存状态"""
        with open(self.file_path, 'w', encoding='utf-8') as f:
            json.dump(states, f, ensure_ascii=False, indent=2)
    
    def update(self, symbol: str, position: str, price: float, ma20: float):
        """更新单个标的状态"""
        states = self.load()
        states[symbol] = {
            "position": position,
            "price": price,
            "ma20": ma20,
            "updated_at": datetime.now().isoformat()
        }
        self.save(states)
```

---

## 3. 主流程实现

### 3.1 入口文件 (main.py)

```python
import os
import sys
import yaml
import schedule
import time
from datetime import datetime
from pathlib import Path

# 添加项目根目录到路径
sys.path.insert(0, str(Path(__file__).parent))

from config.loader import ConfigLoader
from core.fetcher import StockFetcher
from core.indicator import TechnicalIndicator
from core.detector import SignalDetector
from notification.telegram import TelegramNotifier
from storage.state import StateStorage


class StockMonitor:
    """股票均线监控主类"""
    
    def __init__(self, config_path: str = "config.yaml"):
        # 加载配置
        config = ConfigLoader.load(config_path)
        
        # 初始化组件
        self.fetcher = StockFetcher()
        self.indicator = TechnicalIndicator()
        self.detector = SignalDetector()
        self.storage = StateStorage()
        
        # 通知器
        notif_cfg = config['notification']['telegram']
        self.notifier = TelegramNotifier(
            token=os.getenv('TELEGRAM_TOKEN', notif_cfg.get('token', '')),
            chat_id=os.getenv('TELEGRAM_CHAT_ID', notif_cfg.get('chat_id', ''))
        )
        
        # 监控配置
        self.interval = config['monitor']['interval']
        self.stocks = config['stocks']
        self.etfs = config['etfs']
        self.watchlist = self.stocks + self.etfs
        
        print(f"✅ 监控服务初始化完成")
        print(f"   监控标的: {len(self.watchlist)} 只")
        print(f"   监控间隔: {self.interval} 秒")
    
    def check_one(self, symbol: str) -> dict:
        """检查单只股票"""
        try:
            # 获取实时价格
            price = self.fetcher.get_realtime_price(symbol)
            
            # 获取历史数据计算MA20
            hist = self.fetcher.get_historical_prices(symbol)
            ma20 = self.indicator.calculate_ma(hist['收盘'], period=20)
            
            return {
                'success': True,
                'price': price,
                'ma20': ma20
            }
        except Exception as e:
            print(f"   ⚠️ {symbol} 获取数据失败: {e}")
            return {'success': False, 'error': str(e)}
    
    def run(self):
        """执行一次监控检查"""
        print(f"\n[{datetime.now().strftime('%Y-%m-%d %H:%M:%S')}] 开始监控检查...")
        
        # 加载历史状态
        states = self.storage.load()
        
        for symbol in self.watchlist:
            # 获取数据
            result = self.check_one(symbol)
            if not result['success']:
                continue
            
            price = result['price']
            ma20 = result['ma20']
            previous_state = states.get(symbol, {}).get('position')
            
            # 检测信号
            signal = self.detector.detect(
                symbol=symbol,
                current_price=price,
                ma20=ma20,
                previous_position=previous_state
            )
            
            # 发送通知
            if signal:
                print(f"   🔔 {signal.direction}: {symbol} - {price:.2f} vs MA20 {ma20:.2f}")
                try:
                    self.notifier.send_signal(signal)
                except Exception as e:
                    print(f"   ❌ 通知发送失败: {e}")
            
            # 更新状态
            current_position = "above" if price > ma20 else "below"
            self.storage.update(symbol, current_position, price, ma20)
        
        print(f"   ✅ 本轮检查完成")
    
    def start(self):
        """启动定时监控"""
        # 立即执行一次
        self.run()
        
        # 设置定时任务
        schedule.every(self.interval).seconds.do(self.run)
        
        print(f"\n🚀 监控服务已启动，每 {self.interval} 秒执行一次\n")
        
        while True:
            schedule.run_pending()
            time.sleep(1)


if __name__ == "__main__":
    monitor = StockMonitor()
    monitor.start()
```

---

## 4. 配置示例

### 4.1 config.yaml

```yaml
# 监控配置
monitor:
  interval: 600  # 10分钟 = 600秒

# 股票列表（A股代码）
stocks:
  - "600519"   # 贵州茅台
  - "000001"   # 平安银行
  - "300750"   # 宁德时代
  - "600036"   # 招商银行
  - "601318"   # 中国平安

# ETF列表
etfs:
  - "510300"   # 沪深300ETF
  - "510500"   # 中证500ETF
  - "159915"   # 创业板ETF
  - "513100"   # 纳指ETF
  - "513500"   # 标普500ETF

# 通知配置
notification:
  type: "telegram"
  telegram:
    # 生产环境使用环境变量
    token: "${TELEGRAM_TOKEN}"
    chat_id: "${TELEGRAM_CHAT_ID}"
```

### 4.2 环境变量 (.env)

```bash
# Telegram Bot 配置
TELEGRAM_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
```

---

## 5. 部署方案

### 5.1 本地运行

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置环境变量
export TELEGRAM_TOKEN="your_token"
export TELEGRAM_CHAT_ID="your_chat_id"

# 3. 运行
python main.py
```

### 5.2 Docker 部署

```dockerfile
FROM python:3.10-slim

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install -r requirements.txt

# 复制代码
COPY . .

# 运行
CMD ["python", "main.py"]
```

```yaml
# docker-compose.yaml
version: '3.8'
services:
  stock-monitor:
    build: .
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
    restart: unless-stopped
```

### 5.3 VPS/Railway 部署

- **Railway**: 免费额度足够个人使用，配置环境变量即可
- **Render**: 支持 Docker 部署
- **VPS**: 使用 systemd 管理进程

```ini
# stock-monitor.service (systemd)
[Unit]
Description=Stock MA20 Monitor
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/opt/stock-monitor
Environment=TELEGRAM_TOKEN=xxx
Environment=TELEGRAM_CHAT_ID=xxx
ExecStart=/usr/bin/python3 /opt/stock-monitor/main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

---

## 6. Telegram Bot 配置指南

### 6.1 创建 Bot

1. 打开 Telegram，搜索 `@BotFather`
2. 发送 `/newbot` 命令
3. 按提示输入 Bot 名称和用户名
4. 获取 Bot Token（格式：`123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`）

### 6.2 获取 Chat ID

1. 搜索刚创建的 Bot，点击 Start
2. 访问：`https://api.telegram.org/bot<TOKEN>/getUpdates`
3. 在 JSON 响应中找到 `chat` -> `id`，这就是你的 Chat ID

### 6.3 测试通知

```bash
# 发送测试消息
curl -X POST "https://api.telegram.org/bot<TOKEN>/sendMessage" \
  -d "chat_id=<CHAT_ID>" \
  -d "text=测试消息：监控服务已启动"
```

---

## 7. 依赖清单 (requirements.txt)

```
# 核心依赖
akshare>=1.12.0        # A股数据获取（数据源：新浪/东方财富）
pandas>=2.0.0          # 数据处理
pyyaml>=6.0           # 配置文件解析

# 通知
requests>=2.28.0       # HTTP 请求

# 定时任务
schedule>=1.2.0       # 定时任务调度

# 工具
python-dotenv>=1.0.0  # 环境变量加载
```

---

## 8. 监控指标与告警

### 8.1 系统健康检查

| 指标 | 阈值 | 处理 |
|------|------|------|
| 单次请求耗时 | > 30秒 | 记录警告日志 |
| API 调用失败 | 连续3次 | 发送告警通知 |
| 状态文件损坏 | - | 自动重建状态 |

### 8.2 通知内容模板

**突破买入信号：**
```
🚀 600519 突破20日均线

• 现价: `1850.50`
• MA20: `1820.30`
• 时间: 2026-03-12 10:30:00
```

**跌破卖出信号：**
```
🔻 510300 跌破20日均线

• 现价: `3.85`
• MA20: `3.92`
• 时间: 2026-03-12 10:30:00
```

---

## 9. 性能优化建议

1. **缓存优化**: 对同一天的历史数据做内存缓存，避免重复请求
2. **并发获取**: 使用 `asyncio` 或 `concurrent.futures` 并发获取多只股票数据
3. **失败重试**: 实现指数退避重试机制
4. **增量更新**: 仅在交易时间段（9:30-15:00）执行检查

---

## 10. 后续扩展

| 功能 | 实现方案 |
|------|----------|
| 微信通知 | 使用企业微信机器人 Webhook |
| 多周期均线 | 增加 MA5/MA10/MA60 计算 |
| K线图表 | 使用 mplfinance 生成图片发送到 Telegram |
| 策略回测 | 基于历史数据进行回测验证 |
| Web UI | 使用 FastAPI 提供管理界面 |
